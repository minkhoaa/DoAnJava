- Người dùng muốn test thử có thể thay đổi file execute.txt thành file docker-compose.yml và chạy với docker
- Hoặc có thể đổi file environment.txt thành .env để chạy chương trình với các ide như visual studio code
